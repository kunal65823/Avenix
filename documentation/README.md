# Avenix Documentation

Documentation site for Avenix — a modern Python tracing library for AI and LLM requests.

This site is built with React and Vite for a fast developer experience. The project structure includes a small ESLint config and development tooling — you can safely ignore the template notes.

For development:

```bash
cd documentation
npm install
npm run dev
```
